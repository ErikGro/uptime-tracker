# Static, deploy-time configuration. Copy to .env and edit.
# All runtime/operational settings (poll interval, webhook, etc.) live in
# the SQLite settings table and are edited via the dashboard.

ADMIN_USER=admin
ADMIN_PASS=changeme
LISTEN_ADDR=:8080
DB_PATH=./uptime.db
